<h1>Welcome!</h1>
<h3>Here are the user pages:</h3>

<% @users.each do |user| %>
  <p><%= link_to user.username + ":" + user.title, user %></p>
<% end %>

<p>Here is the environment variable that should be visible: <%= ENV['ADMIN_PASS'] %></p>
